import React from 'react';
import { Box } from '@mui/system';

export const NotFound = () => {
  return (
    <Box sx={{display: 'block', ml: 'auto', mr: 'auto', fontSize: 30}}>
        NotFound
    </Box>
  );
};
